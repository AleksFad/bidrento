<form action="" method="POST">
    <div>
        <input type="text" name="title" value="{{ post.title|default('') }}">
    </div>
    <textarea name="content" cols="60" rows="30">{{ post.content|default('') }}</textarea>
    <div>
        <button>Edit</button>
    </div>
</form>